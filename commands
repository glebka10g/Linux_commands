Флаги:
-k= спросит ssh password
-K=sudo password
-i inventory
-m ping=ping nodes
ansible nodes -i inventory.ini -m ping - ansible соединяется по ssh проверяет что ansible может подключиться, установлен Python.
become: true = делать все под root
update_cache: yes = apt update
state: present - должен быть
state: absent - не должно быть, удалить


register: result
- debug:
    var: result.stdour -- register проверка
